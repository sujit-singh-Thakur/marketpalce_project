<h1>Contractor dashboard</h1>


<%= link_to "Add new task",new_task_url %><br>
<%= link_to "Add new category",new_category_url %>

<br>
<br>
<table>
<thead>
<tr>
    <td>task name</td>
    <td>category name</td>
    <td>Actions</td>
</tr>
</thead>


 <tbody>
<% @all_contractor.each do |first|%>
<tr>
<td><%= first.description%></td> 
<td><%= first.category.category_name%> </td>
<td><%= link_to 'Edit', edit_admin_task_url(first.id)%></td>
<td> <%= button_to 'Delete', admin_task_path(first.id), method: :delete, :onclick => "return confirm('Are you sure you want to delete this task?')" %></td>

</tr>
<%end%>

 <tbody>

    </table>